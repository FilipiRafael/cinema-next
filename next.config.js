/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}

module.exports = {
  images: {
    domains: ['image.tmdb.org'],
  },
  nextConfig
}

// module.exports = nextConfig

module.exports = nextConfig
